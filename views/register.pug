extends layout



block content
  h1= "회원가입페이지"
  form(action='/api/register', method='POST')
    div
      | 필수 정보를 입력해주세요.
    div
      input(type="text", name="id", placeholder="아이디*(4~20자)", required)
    div
      input(type="password", name="password", placeholder="비밀번호*(영문+숫자, 8~20자)", required)
    div
      input(type="password", name="password_check", placeholder="비밀번호 재확인*", required)
    div
      input(type="text", name="email_id", placeholder="이메일 앞자리*", required)
      | @
      input(type="text", name="email_provider", placeholder="이메일 뒺자리*", required)
    div
      select
        option(value="") 직접 입력
        option(value="naver.com") naver.com
        option(value="gmail.com") gmail.com
    div
      input(type="text", name="name", placeholder="이름*", required)
    div
      input(type="text", name="phone", placeholder="휴대폰*", required)
      button(type="button") 인증받기
    div
      input(type="text", placeholder="인증번호 6자리 입력")
      button(type="button") 확인
    div
      input(type="checkbox")
      | 선택 정보를 입력하시겠어요? 
      a(href="/register") 내용보기
    div 
      input(type="text", name="postNumber",placeholder="우편번호")
      |
      input(type="button", value="주소찾기")
    div
      input(type="text", name="address", placeholder="주소")
    div
      input(type="text", name="detail", placeholder="상세 주소")
    

    div
      input(type="checkbox", id="allowAll", onclick="checkboxCheck(this)")
      | 전체 약관에 동의합니다 
    div  
      input(type="checkbox", id="allowRequired")
      | 필수 항목에 동의합니다
    div
      a(href="/register") 배민사장님광장 이용약관
      |
      a(href="/register") 개인정보 수집이용 동의
    div
      input(type="checkbox", id="allowAdvertising")
      | 광고성 정보 수신 동의 (선택) 
    div 
      a 만 14세 이상 회원만 가입 가능합니다.
      |
      a(href="/register") 내용보기

    input(type='submit', value='가입완료') 
